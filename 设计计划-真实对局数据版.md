
---

# LOLCAA 设计计划（真实对局数据版）

> 项目：LOL Comp Analysis Assistant  
> 阶段：MVP（Red Side Last Pick 推荐）  
> 技术主线：Spring Boot 3 + Java 21 + PostgreSQL + Elasticsearch

---

## 1. 背景与目标

### 输入
- 已 ban 10 个英雄
- 我方已选 4 个（位置已知）
- 敌方已选 5 个（位置已知）
- 我方缺失位置（requiredRole）

### 输出（TopN）
- 英雄 ID / 名称
- 胜率（winRate）
- 样本量（games）
- 使用的降级层级（levelUsed）
- 证据说明（evidence）
- 降级原因（fallbackReason）
- 置信度（confidence）

### 核心理念
- 以**真实对局数据**为唯一依据
- ES 多维过滤 + 聚合统计
- **退火/降级查询**解决阵容组合稀疏性

---

## 2. 技术选型与职责

| 组件 | 用途 |
|------|------|
| PostgreSQL | 英雄静态信息、请求/分析记录、采集任务状态 |
| Elasticsearch | 对局明细索引（主存）、terms 聚合 |
| Spring Boot | 在线推荐 API + 数据采集调度 |

> 对局明细**只存 ES**，PG 不存。

---

## 3. 数据来源与落地策略

### 数据源（MVP）
- **Riot API**（优先）
- 采样 10 万级以上对局
- 不依赖官方聚合接口

### 存储策略
- ES：`lol_matches`
- PG：
  - `champion`
  - `draft_analysis`
  - `ingest_job`（可选）
  - `processed_match`（可选）

---

## 4. ES 索引设计（核心）

### 索引名
`lol_matches`

### 设计原则
- **阵营视角双写**：1 对局 → 2 条文档（RED / BLUE）
- **位置即字段**
- **敌方信息内嵌**
- **胜负即标签**

### 最终 Mapping（MVP）

```json
{
  "matchId": { "type": "keyword" },
  "side": { "type": "keyword" },
  "win": { "type": "boolean" },

  "top": { "type": "keyword", "eager_global_ordinals": true },
  "jungle": { "type": "keyword", "eager_global_ordinals": true },
  "mid": { "type": "keyword", "eager_global_ordinals": true },
  "adc": { "type": "keyword", "eager_global_ordinals": true },
  "sup": { "type": "keyword", "eager_global_ordinals": true },

  "enemyTop": { "type": "keyword", "eager_global_ordinals": true },
  "enemyJungle": { "type": "keyword", "eager_global_ordinals": true },
  "enemyMid": { "type": "keyword", "eager_global_ordinals": true },
  "enemyAdc": { "type": "keyword", "eager_global_ordinals": true },
  "enemySup": { "type": "keyword", "eager_global_ordinals": true }
}
```

### 明确不存储的维度（MVP）
- 版本号
- 地区
- 段位
- 时间戳

> 目的：提高样本密度，降低降级概率。

---

## 5. 退火/降级查询策略（核心）

### 5.1 降级层级表

| 层级 | 我方固定 | 敌方固定 | 样本阈值 | 触发场景 |
|------|----------|----------|----------|-----------|
| P0 | 全部4个位置 | 全部5个 | 3000 | 最严格 |
| P1 | 2个核心位置 | 全部5个 | 5000 | 友方非核心异常 |
| P2 | 2个核心位置 | 3个核心 | 5000 | 敌方非核心异常 |
| P3 | 2个核心位置 | 对位（1个） | 10000 | 组合过稀 |
| P4 | 无 | 对位（1个） | 10000 | 仅对位克制 |
| P5 | 无 | 无 | - | 单英雄基础胜率 |

### 5.2 核心位置映射

#### 友方核心（P1/P2/P3 固定）

| 目标位置 | 核心位置 |
|----------|-----------|
| TOP | MID, JUNGLE |
| MID | JUNGLE, SUP |
| JUNGLE | MID, SUP |
| ADC | JUNGLE, SUP |
| SUP | ADC, JUNGLE |

#### 敌方核心（P2 固定）

| 目标位置 | 核心位置 |
|----------|-----------|
| TOP | TOP, JUNGLE, MID |
| MID | MID, JUNGLE, SUP |
| JUNGLE | JUNGLE, MID, SUP |
| ADC | ADC, SUP, JUNGLE |
| SUP | SUP, ADC, JUNGLE |

### 5.3 降级执行流程（简）

```
P0 → 样本≥3000？ → 返回
  ↓ 否
P1 → 样本≥5000？ → 返回
  ↓ 否
P2 → 样本≥5000？ → 返回
  ↓ 否
P3 → 样本≥10000？ → 返回
  ↓ 否
P4 → 样本≥10000？ → 返回
  ↓ 否
P5（基础胜率）
```

---

## 6. 查询与聚合优化（必须）

### Filter 优先

```json

{"bool": { "filter": [] }}


```

### 聚合参数（固定）

| 参数 | 值 |
|------|-----|
| `execution_hint` | `"map"` |
| `min_doc_count` | 10 |
| `shard_size` | `size × 1.5 + 10` |
| `size` | 20 |

---

## 7. MVP 范围总结

### 包含
- Red Side Last Pick 推荐
- 6 级退火降级
- ES 聚合统计
- PostgreSQL 落业务记录

### 不包含（明确 cut）
- 版本过滤
- 段位过滤
- 地区过滤
- 实时增量更新
- 前端界面（仅 API）

---

## 8. 文档信息

- **版本**：v0.7
- **最后更新**：2026-04-16
- **状态**：可进入开发

---
